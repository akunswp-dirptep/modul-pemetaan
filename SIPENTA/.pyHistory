import requests  url = "https://covid19-public.digitalservice.id/api/v1/sebaran/jabar" response = requests.get(url)  # Periksa status response if response.status_code == 200:     data = response.json()     # Proses data JSON else:     print("Terjadi kesalahan:", response.text)
import requests  url = "https://jsonplaceholder.typicode.com/todos/1" response = requests.get(url)  # Periksa status response if response.status_code == 200:     data = response.json()     # Proses data JSON else:     print("Terjadi kesalahan:", response.text)
url = "https://jsonplaceholder.typicode.com/todos/1" response = requests.get(url)  # Periksa status response if response.status_code == 200:     data = response.json()     # Proses data JSON     print(data) else:     print("Terjadi kesalahan:", response.text)
import arcpy import json  # Set workspace arcpy.env.workspace = "C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT"  # Get the feature class fc = "Persil.shp"  # Create a feature cursor with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row[0])         # Process the feature JSON
import arcpy import json  # Set workspace arcpy.env.workspace = "C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT\\znt_fgdb.gdb"  # Get the feature class fc = "my_features.shp"  # Create a feature cursor with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row[0])         # Process the feature JSON
import arcpy import json  # Set workspace arcpy.env.workspace = "C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT"  # Get the feature class fc = "Persil Kluster Rev2 Kota Mini Surakarta Tahun 2022.shp"  # Create a feature cursor with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row[0])         # Process the feature JSON
import arcpy import json  # Set workspace arcpy.env.workspace = "C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT"  # Get the feature class fc = "Persil Kluster Rev2 Kota Mini Surakarta Tahun 2022.shp"  # Create a feature cursor with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row[0])         print(feature_json)         # Process the feature JSON
import arcpy import json  # Set workspace arcpy.env.workspace = "C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT"  # Get the feature class fc = "Persil Kluster Rev2 Kota Mini Surakarta Tahun 2022.shp"  # Create a feature cursor with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row)         print(feature_json)         # Process the feature JSON
import arcpy import json import requests  # Set workspace arcpy.env.workspace = r"C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT\\znt_fgdb.gdb"  # Akses feature class fc = "znt_ds"  # Buat cursor dan ekstrak data with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row[0])         print(feature_json)
import arcpy import json import requests  # Set workspace arcpy.env.workspace = r"C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT\\znt_fgdb.gdb"  # Akses feature class fc = "Persil"  # Buat cursor dan ekstrak data with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row[0])         print(feature_json)
import arcpy import json import requests  # Set workspace arcpy.env.workspace = r"C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT\\znt_fgdb.gdb"  # Akses feature class fc = "Persil"  # Buat cursor dan ekstrak data with arcpy.da.SearchCursor(fc, ["SHAPE@JSON"]) as cursor:     for row in cursor:         feature_json = json.loads(row[1])         print(feature_json)
import arcpy import json import requests  # Set workspace arcpy.env.workspace = r"C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT\\znt_fgdb.gdb"  # Akses feature class fc = "Persil"  # Buat cursor dan ekstrak data with arcpy.da.SearchCursor(fc, ["SHAPE@JSON","TARGET_FID"]) as cursor:     for row in cursor:         feature_json = json.loads(row[1])         print(feature_json)
import arcpy import json import requests  # Set workspace arcpy.env.workspace = r"C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT\\znt_fgdb.gdb"  # Akses feature class fc = "Persil"  # Buat cursor dan ekstrak data with arcpy.da.SearchCursor(fc, ["SHAPE@JSON","TARGET_FID"]) as cursor:     for row in cursor:         feature_json = json.loads.str((row[1]))         print(feature_json)
import arcpy import json import requests  # Set workspace arcpy.env.workspace = r"C:\\PenilaianTanah\\Geospasial\\Ujicoba Tools NBT\\znt_fgdb.gdb"  # Akses feature class fc = "Persil"  # Buat cursor dan ekstrak data with arcpy.da.SearchCursor(fc, ["SHAPE@JSON","TARGET_FID"]) as cursor:     for row in cursor:         feature_json = json.loads(str(row[1]))         print(feature_json)
import requests  url = 'https://sipenta.atrbpn.go.id/tatausaha/apis/upload' file_path = r'C:\Users\sidiq\Downloads\1727089882_032497996c26ff016fa8.zip'  headers = {     'Content-Type': 'multipart/form-data'  # Sesuaikan dengan header di Postman }  with open(file_path, 'rb') as f:     response = requests.post(url, files={'file': f}, headers=headers)  if response.status_code == 200:     print('File berhasil diunggah!') else:     print('Terjadi kesalahan:', response.text)
url_upload = "https://sipenta.atrbpn.go.id/tatausaha/apis/upload"  fileobj = open(zipname, 'rb')              file_path = r'C:/Users/Febian/Downloads/Zona_Layer.zip'  headers = {'Content-Type': 'multipart/form-data; boundary=<calculated when request is sent>',            'Content-Length': '<calculated when request is sent>',             'Host': '<calculated when request is sent>', 'User-Agent' : '<calculated when request is sent>', 'Accept': '*/*', 'Accept-Encoding': 'gzip, deflate, br', 'Connection': 'keep-alive'}  with open(file_path, 'rb') as f:     response = requests.post(url_upload, files={'file': f}, headers=headers)  arcpy.AddMessage(response.text)
url_upload = "https://sipenta.atrbpn.go.id/tatausaha/apis/upload"              file_path = r'C:/Users/Febian/Downloads/Zona_Layer.zip'  headers = {'Content-Type': 'multipart/form-data; boundary=<calculated when request is sent>',            'Content-Length': '<calculated when request is sent>',             'Host': '<calculated when request is sent>', 'User-Agent' : '<calculated when request is sent>', 'Accept': '*/*', 'Accept-Encoding': 'gzip, deflate, br', 'Connection': 'keep-alive'}  with open(file_path, 'rb') as f:     response = requests.post(url_upload, files={'file': f}, headers=headers)  arcpy.AddMessage(response.text)
import arcpy, os, sys, zipfile, shutil, requests, json, math from os.path import basename
import arcpy, os, sys, zipfile, shutil, requests, json, math from os.path import basename  url_upload = "https://sipenta.atrbpn.go.id/tatausaha/apis/upload"              file_path = r'C:/Users/Febian/Downloads/Zona_Layer.zip'  headers = {'Content-Type': 'multipart/form-data; boundary=<calculated when request is sent>',            'Content-Length': '<calculated when request is sent>',             'Host': '<calculated when request is sent>', 'User-Agent' : '<calculated when request is sent>', 'Accept': '*/*', 'Accept-Encoding': 'gzip, deflate, br', 'Connection': 'keep-alive'}  with open(file_path, 'rb') as f:     response = requests.post(url_upload, files={'file': f}, headers=headers)  arcpy.AddMessage(response.text)
clear
import arcpy, os, sys, zipfile, shutil, requests, json, math from os.path import basename  url_upload = "https://sipenta.atrbpn.go.id/tatausaha/apis/upload"              file_path = r'C:/Users/Febian/Downloads/Zona_Layer.zip'  headers = {'Content-Type': 'multipart/form-data; boundary=<calculated when request is sent>',            'Content-Length': '<calculated when request is sent>',             'Host': '<calculated when request is sent>', 'User-Agent' : '<calculated when request is sent>', 'Accept': '*/*', 'Accept-Encoding': 'gzip, deflate, br', 'Connection': 'keep-alive'}  with open(file_path, 'rb') as f:     response = requests.post(url_upload, files={'file': f}, headers=headers)  arcpy.AddMessage(response.text)
import arcpy, os, sys, zipfile, shutil, requests, json, math from os.path import basename  url_upload = "https://sipenta.atrbpn.go.id/tatausaha/apis/upload"              file_path = r'C:/Users/Febian/Downloads/Zona_Layer.zip'  headers = {'Content-Type': 'multipart/form-data; boundary=<calculated when request is sent>',            'Content-Length': '<calculated when request is sent>',             'Host': '<calculated when request is sent>', 'User-Agent' : '<calculated when request is sent>', 'Accept': '*/*', 'Accept-Encoding': 'gzip, deflate, br', 'Connection': 'keep-alive'}  with open(file_path, 'rb') as f:     response = requests.post(url_upload, files={'file': f}, headers=headers)  print(response.text)
import arcpy, os, sys, zipfile, shutil, requests, json, math
import arcpy, os, sys, zipfile, shutil, requests, json, math from os.path import basename  url_upload = "https://sipenta.atrbpn.go.id/tatausaha/apis/upload"              file_path = r'C:/Users/Febian/Downloads/Zona_Layer.zip'  headers = {'Content-Type': 'multipart/form-data; boundary=<calculated when request is sent>',            'Content-Length': '<calculated when request is sent>',             'Host': '<calculated when request is sent>', 'User-Agent' : '<calculated when request is sent>', 'Accept': '*/*', 'Accept-Encoding': 'gzip, deflate, br', 'Connection': 'keep-alive'}  with open(file_path, 'rb') as f:     response = requests.post(url_upload, files={'file': f}, headers=headers)  print(response.text)
import requests def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}      headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)  print(response.text)          # url ="http://localhost/tatausaha-ptep/public/apis/upload"     url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"          file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"          upload_file(url, file_path)
import requests def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}      headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)          print(response.text)          # url ="http://localhost/tatausaha-ptep/public/apis/upload"     url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"          file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"          upload_file(url, file_path)
import requests def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}      headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)          print(response)          # url ="http://localhost/tatausaha-ptep/public/apis/upload"     url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"          file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"          upload_file(url, file_path)
import requests def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}      headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)          print(str(response))          # url ="http://localhost/tatausaha-ptep/public/apis/upload"     url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"          file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"          upload_file(url, file_path)
import requests def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}              # url ="http://localhost/tatausaha-ptep/public/apis/upload"     url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"      headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)          print(str(response))          file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"          upload_file(url, file_path)
def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}              # url ="http://localhost/tatausaha-ptep/public/apis/upload"     url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"      headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)          print(str(response))      file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"      upload_file(url, file_path)
def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}              headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)          # url ="http://localhost/tatausaha-ptep/public/apis/upload"     url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"          print(str(response))      file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"      upload_file(url, file_path)
import requests def upload_file(url, file_path):     with open(file_path,'rb') as f:         files = {'file': (file_path, f)}              headers = {"Content-Type": "multipart/form-data"}      response = requests.post(url,files=files)          print(str(response))          # url ="http://localhost/tatausaha-ptep/public/apis/upload" url ="https://sipenta.atrbpn.go.id/tatausaha/apis/upload"      file_path ="C:/Users/Febian/Downloads/Zona_Layer.zip"      upload_file(url, file_path)
